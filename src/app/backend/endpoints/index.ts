import { importedClusterList } from "./importedClusterList";
import { rememberCluster } from "./rememberCluster";

const endpoints = {
  importedClusterList,
  rememberCluster,
};

export { endpoints };
